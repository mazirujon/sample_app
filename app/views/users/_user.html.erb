<li>
  <%= gravatar_for user, size: Settings.SIZE_AVARTA %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to "delete", user, method: :delete, data: {confirm: t("messenger.sure_del")} %>
  <% end %>
</li>
